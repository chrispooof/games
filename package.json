{
  "name": "games-monorepo",
  "private": true,
  "workspaces": ["ui", "api"],
  "scripts": {
    "prepare": "husky",
    "dev": "nx run @games/ui:dev",
    "build": "nx run @games/ui:build",
    "test": "nx run-many -t test",
    "typecheck": "nx run-many -t typecheck",
    "lint": "nx run-many -t lint",
    "format": "nx run-many -t format"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "nx": "latest",
    "prettier": "^3.8.1"
  }
}
